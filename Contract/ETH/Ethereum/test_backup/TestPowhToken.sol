pragma solidity ^0.4.24;

import "truffle/Assert.sol";
import "truffle/DeployedAddresses.sol";
import "../contracts/PowhToken.sol";

contract TestPowhToken {
    /*
    function test1() public {
        PowhToken powhToken = new PowhToken();
        Assert.equal(powhToken.totalSupply(), 0, "ok");        
        uint256 ethers = 123456;
        uint256 tokens = powhToken.ethereumToTokens_(ethers);
        Assert.equal(powhToken.tokensToEthereum_(tokens), ethers, "buy and sell should be consistense");
    }
    function test3() public {
        PowhToken powhToken = new PowhToken();
        Assert.equal(powhToken.totalSupply(), 0, "ok");        
        uint256 ethers = 123456888;
        uint256 tokens1 = powhToken.ethereumToTokens_(ethers);
        uint256 tokens2 = powhToken.ethereumToTokens2_(ethers);
        Assert.equal(tokens1, tokens2, "2 buy function should be consistense");
    }    
    function test2() public {
        
        PowhToken powhToken = new PowhToken();
        uint256 ethers = 1e18;
        uint256 tokens = 1e18; //powhToken.ethereumToTokens_(ethers);
        uint256 sellEthers = powhToken.tokensToEthereum_(tokens);
        Assert.equal(sellEthers, ethers, );
        
    }*/
}
